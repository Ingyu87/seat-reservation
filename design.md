# 좌석 예약 시스템 디자인 계획

## 1. 디자인 목표

이 서비스는 홍보용 랜딩페이지가 아니라 실제 행사 운영 도구다. 첫 화면부터 좌석 선택과 예약 진행이 바로 가능해야 한다.

핵심 목표:

- 예약자는 좌석 현황을 빠르게 이해하고 직접 선택할 수 있어야 한다.
- 이미 예약된 좌석과 선택 가능한 좌석이 명확히 구분되어야 한다.
- 2500석 규모에서도 화면이 복잡하거나 느리게 느껴지지 않아야 한다.
- 모바일에서도 좌석 선택, 확대, 예약 입력이 가능해야 한다.
- 예약자는 예약번호 없이 이름, 전화번호 뒤 4자리, 수정 비밀번호로 예약을 조회할 수 있어야 한다.
- 관리자는 이름 검색 후 좌석 위치를 즉시 확인할 수 있어야 한다.

## 2. 화면 구조

```txt
/                     예약 메인
/lookup               예약 조회/변경/취소
/privacy              개인정보 처리방침
관리자 사이트 (별도 배포)
/login                관리자 로그인
/                     관리자 대시보드·예약 검색
```

예약자 화면과 관리자 화면은 시각 톤을 분리한다.

- 예약자 화면: 극장 예매에 가까운 직관적인 UI
- 관리자 화면: 검색, 표, 필터 중심의 실무형 UI

## 3. 예약자 화면

## 3.1 예약 메인

첫 화면 구성:

```txt
[상단 고정 바]
행사명 / 전체 좌석 / 예약 완료 / 잔여 좌석 / 예약 조회 버튼

[좌석 안내]
예약 가능 / 예약 완료 / 선택 좌석 범례

[무대 표시]

[좌석 배치도]
A열 1~50번
B열 1~50번
...

[하단 안내]
좌석을 선택하면 예약 정보를 입력합니다.
```

상단 바:

- 행사명
- 전체 좌석 수
- 예약 완료 수
- 잔여 좌석 수
- `예약 조회` 버튼

좌석 배치도:

- 무대는 상단 중앙에 표시한다.
- 좌석은 작은 정사각형 셀로 표시한다.
- 열 라벨은 좌측에 고정적으로 보이게 한다.
- 좌석 번호는 hover 또는 선택 상태에서 보여준다.
- PC에서는 전체를 최대한 한눈에 보이게 한다.
- 모바일에서는 가로 스크롤과 확대를 허용한다.

## 3.2 좌석 상태 색상

| 상태 | 색상 | 설명 |
| --- | --- | --- |
| 예약 가능 | Green | 클릭 가능 |
| 선택 중 | Blue | 현재 사용자가 선택한 좌석 |
| 예약 완료 | Gray | 클릭 불가 |
| 오류/불가 | Red | 중복 예약 실패 등 |
| 안내/주의 | Orange | 예약 조회 정보 안내 |

권장 색상:

```txt
Primary Green: #2E7D32
Available:     #66BB6A
Selected:      #1565C0
Reserved:      #BDBDBD
Error:         #C62828
Warning:       #E65100
Background:    #F7FAF7
Surface:       #FFFFFF
Text:          #1F2933
Muted Text:    #6B7280
Border:        #DDE5DD
```

## 3.3 예약 입력

좌석 선택 후 표시할 입력 항목:

```txt
선택 좌석: A열 12번

이름 *
전화번호 뒤 4자리 *
이메일 *
수정 비밀번호 *
개인정보 수집 및 이용 동의 *

[예약하기]
```

디자인 원칙:

- 선택 좌석은 입력 폼 상단에 강조한다.
- 전화번호 뒤 4자리는 숫자 4자리만 입력하게 한다.
- 수정 비밀번호에는 “예약 조회, 변경, 취소에 사용됩니다.”라는 보조 문구를 붙인다.
- 개인정보 동의는 체크하지 않으면 예약 버튼을 비활성화한다.
- 에러 메시지는 각 필드 아래에 짧게 표시한다.

## 3.4 예약 완료

예약 완료 화면은 좌석 정보와 조회 방법을 가장 명확하게 보여준다.

```txt
예약이 완료되었습니다.

예약자: 홍길동
좌석: A열 12번
예약일시: 2026.05.20 14:30

이메일로 예약 정보를 발송했습니다.

예약 조회, 변경, 취소 시
이름, 전화번호 뒤 4자리, 수정 비밀번호가 필요합니다.

[예약 조회로 이동]
[좌석 화면으로 돌아가기]
```

중요:

- 예약번호를 크게 보여주는 방식은 사용하지 않는다.
- 좌석 정보와 조회 방법을 강조한다.
- 이메일 발송 실패 시에도 예약은 완료되었음을 명확히 알려준다.

## 4. 예약 조회/변경/취소

조회 화면:

```txt
예약 조회

이름
전화번호 뒤 4자리
수정 비밀번호

[조회하기]
```

조회 성공 후:

```txt
예약자: 홍길동
좌석: A열 12번
예약 상태: 예약 완료

[좌석 변경]
[예약 취소]
```

좌석 변경:

- `좌석 변경`을 누르면 기존 좌석을 안내로 표시한다.
- 좌석 선택 화면으로 이동하되, 기존 예약자 정보는 유지한다.
- 새 좌석을 선택하고 변경 확정 버튼을 누르면 서버에서 중복 여부를 확인한다.
- 변경 완료 후 이메일을 다시 발송한다.

예약 취소:

```txt
예약을 취소하시겠습니까?
A열 12번 좌석 예약이 취소됩니다.

[아니오]
[예약 취소]
```

취소 완료 후:

- 취소 완료 메시지 표시
- 취소 안내 이메일 발송

## 5. 관리자 화면

관리자 화면은 빠른 확인과 실수 방지가 중요하다.

## 5.1 관리자 로그인

```txt
관리자 로그인

이메일
비밀번호

[로그인]
```

## 5.2 관리자 대시보드

```txt
[상단]
행사 좌석 예약 관리자 / 로그아웃

[통계]
전체 좌석 2500
예약 완료 1240
잔여 좌석 1260
취소 건수 12

[검색]
이름, 전화번호 뒤 4자리, 이메일, 좌석 검색

[최근 예약 목록]
```

통계 카드는 4개 정도로 제한한다.

- 전체 좌석
- 예약 완료
- 잔여 좌석
- 취소 건수

## 5.3 관리자 예약 검색

검색 입력:

```txt
이름, 전화번호 뒤 4자리, 이메일, 좌석으로 검색
```

검색 결과 테이블 컬럼:

- 이름
- 전화번호 뒤 4자리
- 이메일
- 좌석
- 상태
- 예약일시
- 관리

관리 버튼:

- 상세
- 좌석 변경
- 취소

테이블 디자인:

- 한 화면에 많은 정보를 볼 수 있게 행 높이를 낮춘다.
- 전화번호 뒤 4자리와 이메일은 관리자에게만 표시한다.
- 상태는 작은 배지로 표시한다.
- 예약 완료: Green
- 취소됨: Gray

## 5.4 예약자 상세

```txt
예약자 상세

이름: 홍길동
전화번호 뒤 4자리: 5678
이메일: user@example.com
좌석: A열 12번
상태: 예약 완료
예약일시: 2026.05.20 14:30
수정일시: 2026.05.20 14:30

[이메일 재발송]
[좌석 변경]
[예약 취소]
```

위험한 작업인 취소는 빨간 버튼으로 표시하고 확인 모달을 거친다.

## 6. 반응형 디자인

PC:

- 좌석 맵과 예약 정보가 넓게 보이도록 `max-width`를 크게 둔다.
- 좌석 배치도는 화면 중앙에 배치한다.
- 관리자 테이블은 넓은 화면을 활용한다.

Tablet:

- 좌석 맵은 가로 스크롤을 허용한다.
- 예약 폼은 모달 또는 하단 패널로 표시한다.

Mobile:

- 좌석 맵은 확대/스크롤 가능한 영역으로 제공한다.
- 선택 좌석 정보는 하단 고정 바에 표시한다.
- 예약 입력은 전체 화면 바텀시트 또는 별도 페이지로 전환한다.
- 관리자 테이블은 카드형 리스트로 전환한다.

## 7. 접근성

- 색상만으로 상태를 구분하지 않는다.
- 좌석 셀에 `aria-label`을 제공한다.
- 예약 완료 좌석은 `disabled` 상태로 처리한다.
- 입력 필드에는 명확한 label을 제공한다.
- 에러 메시지는 필드 근처에 표시한다.

좌석 aria-label 예:

```txt
A열 12번, 예약 가능
A열 13번, 예약 완료
```

## 8. UI 컴포넌트 목록

```txt
Layout
Header
StatusSummary
SeatLegend
StageBanner
SeatMap
SeatRow
SeatCell
ReservationDrawer
ReservationForm
ReservationComplete
LookupForm
ReservationDetail
ConfirmDialog
AdminLayout
AdminLoginForm
AdminStats
AdminSearchBar
AdminReservationTable
AdminReservationDetail
Toast
```

## 9. 문구 스타일

좋은 예:

- 좌석을 선택해주세요.
- 이미 예약된 좌석입니다.
- 예약이 완료되었습니다.
- 예약 조회에는 이름, 전화번호 뒤 4자리, 수정 비밀번호가 필요합니다.
- 수정 비밀번호는 예약 조회, 변경, 취소에 사용됩니다.

피해야 할 예:

- 시스템 처리 중 오류가 발생했습니다.
- 잘못된 요청입니다.
- 실패했습니다.

대신:

- 예약 중 문제가 발생했습니다. 잠시 후 다시 시도해주세요.
- 방금 다른 사용자가 예약한 좌석입니다. 다른 좌석을 선택해주세요.

## 10. MVP 디자인 범위

1. 예약 메인 좌석 선택 화면
2. 예약 입력 모달 또는 페이지
3. 예약 완료 화면
4. 예약 조회 화면
5. 좌석 변경 화면
6. 예약 취소 확인 모달
7. 관리자 로그인
8. 관리자 대시보드
9. 관리자 예약 검색 테이블
10. 관리자 예약 상세

## 11. 와이어프레임

예약 메인:

```txt
┌────────────────────────────────────────────┐
│ 행사명                  예약 조회          │
│ 전체 2500 | 예약 300 | 잔여 2200          │
├────────────────────────────────────────────┤
│  ■ 가능   ■ 선택   ■ 완료                 │
│                                            │
│              [  무대  ]                    │
│                                            │
│ A  □ □ □ □ □ □ □ □ □ □ ...                │
│ B  □ □ □ □ □ □ □ □ □ □ ...                │
│ C  □ □ □ □ □ □ □ □ □ □ ...                │
└────────────────────────────────────────────┘
```

예약 입력:

```txt
┌──────────────────────────┐
│ 좌석 예약                │
│ 선택 좌석: A열 12번      │
│                          │
│ 이름 *                   │
│ [                    ]   │
│ 전화번호 뒤 4자리 *      │
│ [ 5678               ]   │
│ 이메일 *                 │
│ [                    ]   │
│ 수정 비밀번호 *          │
│ [                    ]   │
│ □ 개인정보 수집 동의     │
│                          │
│ [예약하기]               │
└──────────────────────────┘
```

관리자 검색:

```txt
┌────────────────────────────────────────────┐
│ 관리자                              로그아웃 │
├────────────────────────────────────────────┤
│ 전체 2500  예약 1240  잔여 1260  취소 12 │
│                                            │
│ [이름, 전화번호 뒤 4자리, 이메일, 좌석 검색] │
│                                            │
│ 이름 | 전화번호 뒤 4자리 | 이메일 | 좌석 | 상태 │
│ 홍길동 | 5678 | user@... | A-12 | 완료 │
│ 김영희 | 1234 | user@... | B-08 | 완료 │
└────────────────────────────────────────────┘
```

## 12. 구현 시 주의사항

- 좌석 셀은 크기가 흔들리지 않도록 고정 width/height를 사용한다.
- 2500개 좌석 렌더링은 가능하지만 불필요한 재렌더링을 줄여야 한다.
- `SeatCell`은 memo 처리한다.
- 관리자 테이블은 페이지네이션 또는 검색 결과 제한을 둔다.
- 모바일에서 버튼 텍스트가 줄 밖으로 나가지 않도록 최소 너비와 줄바꿈을 고려한다.
- 예약 완료 화면에서 좌석 정보와 조회 방법이 가장 눈에 띄어야 한다.
- 관리자 화면에서는 장식보다 정보 밀도와 정렬을 우선한다.
