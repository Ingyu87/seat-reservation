{
  "name": "seat-reservation",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev:booking",
    "dev:booking": "npm run dev --workspace @seat/booking",
    "dev:admin": "npm run dev --workspace @seat/admin",
    "build": "npm run build --workspace @seat/booking && npm run build --workspace @seat/admin",
    "build:booking": "npm run build --workspace @seat/booking",
    "build:admin": "npm run build --workspace @seat/admin",
    "typecheck": "npm run typecheck --workspace @seat/booking && npm run typecheck --workspace @seat/admin"
  }
}
